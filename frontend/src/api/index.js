// Change this
const API_BASE_URL = 'http://localhost:8000/api'

// To this (if using default Flask port)
const API_BASE_URL = 'http://localhost:5000/api'

// Or use relative URLs if proxy is configured
const API_BASE_URL = '/api'
